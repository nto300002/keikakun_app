# Issue: Gmail期限通知バッチ処理のスケーラビリティ改善

**Issue #XXX: Optimize deadline notification batch performance for 500+ offices**

---

## 📖 背景・課題

### 現状の問題

現在の実装では、事業所数が増加すると処理時間が線形に増加し、メモリ使用量も増大します。

| 事業所数 | 現在の処理時間 | 問題点 |
|---------|---------------|--------|
| 10 | 30秒 | 問題なし |
| 100 | 5分 | やや遅い |
| 500 | **25分** ⚠️ | 実用的でない |
| 1,000 | **50分** ⚠️ | タイムアウトリスク |

### 特定された課題

1. **N+1クエリ問題**: 事業所ごとにアラート取得・スタッフ取得を実行（O(N)クエリ）
2. **直列処理**: 事業所処理が順次実行される
3. **メモリ非効率**: 全データを一度にメモリ展開

#### 課題の詳細分析

**1. N+1クエリ問題**
```python
# 現在の実装（問題あり）
for office in offices:  # 500回ループ
    # 毎回個別にクエリ（500回のクエリ）
    alerts = await WelfareRecipientService.get_deadline_alerts(
        db=db,
        office_id=office.id
    )
    # 毎回個別にクエリ（500回のクエリ）
    staffs = await get_staffs(db, office.id)
```

**影響:**
- 500事業所の場合: 1,000回以上のDBクエリ
- クエリ時間: 1,000回 × 10ms = 10秒（クエリだけで）

**2. 直列処理の問題**
```python
# 事業所Aの処理（完了を待つ）
await process_office_A()
# 事業所Bの処理（Aの完了後）
await process_office_B()
# ...
```

**影響:**
- 500事業所 × 3秒/事業所 = 1,500秒（25分）
- CPUアイドル時間が多い（I/O待ち）

**3. メモリ非効率**
```python
# 全事業所を一度にメモリに展開
offices = result.scalars().all()  # 500事業所 × 100KB = 50MB
staffs = result.scalars().all()   # 5,000スタッフ × 50KB = 250MB
```

**影響:**
- ピークメモリ: 500MB以上
- GCオーバーヘッド増加

---

## 🎯 目標

**500事業所規模で5分以内に処理完了**し、メモリ使用量を1/10に削減する。

### パフォーマンス目標

| メトリクス | 現在 | 目標 | 改善率 |
|-----------|------|------|--------|
| 処理時間（500事業所） | 25分 | **5分以内** | 5倍 |
| DBクエリ数（500事業所） | 1,000+ | **10以下** | 100倍 |
| メモリ使用量（500事業所） | 500MB | **50MB以下** | 10倍 |
| 同時処理可能数 | 5件（メールのみ） | **50件（事業所+メール）** | 10倍 |

### ビジネス上の目標

- **運用コスト削減**: サーバーリソース使用量の削減
- **ユーザー体験向上**: 朝9時の通知がより確実に届く
- **スケーラビリティ確保**: 1,000事業所以上に対応可能な基盤

---

## ✅ 受け入れ基準

### 必須要件

- [ ] 既存の全テストがパスする（後方互換性）
- [ ] 500事業所、5,000スタッフの負荷テストで5分以内に完了
- [ ] メモリ使用量が50MB以下
- [ ] DBクエリ数が事業所数に比例しない（O(1)またはO(log N)）
- [ ] dry_runモードが正常動作

### 品質要件

- [ ] コードカバレッジ85%以上維持
- [ ] パフォーマンステストが自動化されている
- [ ] 監査ログが正確に記録される
- [ ] エラーハンドリングが適切

### パフォーマンス要件詳細

#### 処理時間

| 事業所数 | スタッフ数 | 目標時間 | 許容時間 |
|---------|-----------|---------|---------|
| 10 | 100 | 10秒 | 30秒 |
| 100 | 1,000 | 1分 | 3分 |
| 500 | 5,000 | 3分 | 5分 |
| 1,000 | 10,000 | 5分 | 10分 |

#### DBクエリ効率

| 操作 | 現在のクエリ数 | 目標クエリ数 |
|------|--------------|-------------|
| 事業所取得 | 1 | 1 |
| アラート取得 | N（事業所数） | 2（一括取得） |
| スタッフ取得 | N（事業所数） | 1（一括取得） |
| **合計** | **2N + 1** | **4（定数）** |

#### メモリ効率

- **ピークメモリ**: 50MB以下
- **平均メモリ**: 30MB以下
- **メモリリーク**: なし（GC後に80%以上回収）

---

## 🔧 技術要件

### 最適化項目

1. **バッチクエリ化**: N+1問題の解消
   - `get_deadline_alerts_batch()` 実装
   - `get_staffs_by_offices_batch()` 実装

2. **事業所並列処理**: asyncio.gather()使用
   - Semaphore(10)で同時10事業所まで並列実行

3. **チャンク処理**: メモリ効率化
   - 100事業所ごとにバッチ処理

4. **接続プール最適化**: セッション管理改善
   - 事業所ごとに独立したセッション（オプション）

### アーキテクチャ変更

#### 変更前
```
for office in offices:          # 直列処理
    alerts = get_alerts(office)  # N回クエリ
    staffs = get_staffs(office)  # N回クエリ
    for staff in staffs:
        send_email(staff)        # 並列（5件まで）
```

#### 変更後
```
# Phase 1: バッチクエリ
alerts_by_office = get_alerts_batch(office_ids)  # 2回クエリ
staffs_by_office = get_staffs_batch(office_ids)  # 1回クエリ

# Phase 2: 並列処理
async def process_office(office_id):
    alerts = alerts_by_office[office_id]
    staffs = staffs_by_office[office_id]
    for staff in staffs:
        send_email(staff)

# 並列実行（10事業所まで同時）
await asyncio.gather(*[
    process_office(office_id)
    for office_id in office_ids
])
```

### 非機能要件

- **後方互換性**: 既存APIとの互換性維持
- **ログ**: ログレベル・フォーマット維持
- **監査**: 監査ログの完全性保証
- **エラーハンドリング**: エラーリカバリー機能維持
- **可観測性**: メトリクス・トレーシング追加

---

## 📊 期待される効果

### 処理時間の改善

```
現在: 500事業所 × 3秒 = 1,500秒（25分）
改善後: 500事業所 / 10並列 × 3秒 = 150秒（2.5分）

改善率: 10倍高速化
```

### DBクエリ数の改善

```
現在:
- 事業所取得: 1クエリ
- アラート取得: 500クエリ（事業所ごと）
- スタッフ取得: 500クエリ（事業所ごと）
- 合計: 1,001クエリ

改善後:
- 事業所取得: 1クエリ
- アラート一括取得: 2クエリ（更新期限 + アセスメント）
- スタッフ一括取得: 1クエリ
- 合計: 4クエリ

改善率: 250倍削減
```

### メモリ使用量の改善

```
現在:
- 全事業所データ: 50MB
- 全スタッフデータ: 250MB
- 全アラートデータ: 200MB
- 合計: 500MB

改善後（チャンク処理）:
- チャンク単位データ: 30MB
- メモリ効率化: 10倍改善

改善率: 10倍削減
```

---

## 🚨 リスクと対策

### リスク1: 既存機能の破壊

**リスク**: 最適化により既存の動作が変わる可能性

**対策**:
- TDDアプローチで既存テストを全て実行
- 回帰テストスイートを充実
- カナリアデプロイで段階的にロールアウト

### リスク2: 並列処理のデッドロック

**リスク**: データベース接続プールの枯渇

**対策**:
- Semaphoreで並列度を制御（10並列まで）
- 接続プール設定を確認（最小20接続以上）
- タイムアウト設定（30秒）

### リスク3: メモリリーク

**リスク**: チャンク処理でメモリが解放されない

**対策**:
- メモリ効率テストを追加
- GC動作を検証
- プロファイリングツールで監視

---

## 📅 実装計画

### Phase 1: パフォーマンステスト追加（1日）

- [ ] パフォーマンステストファイル作成
- [ ] 500事業所テストデータ生成ヘルパー
- [ ] クエリカウンター実装
- [ ] メモリプロファイラー統合

### Phase 2: バッチクエリ実装（2日）

- [ ] `get_deadline_alerts_batch()` 実装
- [ ] `get_staffs_by_offices_batch()` 実装
- [ ] 単体テスト作成
- [ ] 統合テスト実行

### Phase 3: 並列処理実装（1日）

- [ ] `_process_single_office()` 関数分離
- [ ] `asyncio.gather()` 統合
- [ ] Semaphore設定
- [ ] エラーハンドリング実装

### Phase 4: テスト・検証（1日）

- [ ] 全テストスイート実行
- [ ] パフォーマンステスト検証
- [ ] メモリリークテスト
- [ ] 負荷テスト実行

### Phase 5: ドキュメント・リリース（0.5日）

- [ ] CHANGELOG更新
- [ ] アーキテクチャ図更新
- [ ] デプロイ手順作成
- [ ] モニタリング設定

**合計見積もり: 5.5日**

---

## 🔍 モニタリング

### 監視すべきメトリクス

1. **処理時間**
   - バッチ処理全体の実行時間
   - 事業所あたりの平均処理時間

2. **リソース使用量**
   - CPU使用率
   - メモリ使用量（ピーク・平均）
   - DB接続数

3. **エラー率**
   - メール送信失敗率
   - Push通知失敗率
   - タイムアウト発生率

4. **ビジネスメトリクス**
   - 送信成功メール数
   - 処理された事業所数
   - 通知受信率

### アラート設定

| メトリクス | 閾値 | アクション |
|-----------|------|-----------|
| 処理時間 | > 10分 | Slack通知 + PagerDuty |
| メモリ使用量 | > 100MB | Slack通知 |
| エラー率 | > 5% | PagerDuty |
| DB接続数 | > 50 | Slack通知 |

---

## 📚 参考資料

- [現在の実装](../../k_back/app/tasks/deadline_notification.py)
- [スケーラビリティ分析](./scalability_analysis.md)
- [パフォーマンス要件](./performance_requirements.md)
- [実装計画詳細](./implementation_plan.md)
- [テスト仕様書](./test_specifications.md)

---

## 👥 担当者

- **担当**: Backend Team
- **レビュワー**: Tech Lead, SRE Team
- **承認者**: CTO

---

## 📝 変更履歴

| 日付 | 変更内容 | 担当者 |
|------|---------|--------|
| 2026-02-08 | Issue作成 | Claude |
| - | - | - |
